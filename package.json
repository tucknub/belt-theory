{
  "name": "belt-theory",
  "version": "1.2.0",
  "private": true,
  "description": "Championship history, measured — Authentic Archive review build.",
  "scripts": {
    "build": "bash build.sh"
  }
}
