import { readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url));
const root=path.resolve(here,'..');
const out=path.resolve(process.argv[2]||path.join(root,'dist'));
const origin='https://belt-theory.tucknub.workers.dev';
const archive=JSON.parse(await readFile(path.join(root,'data','archive-assets.json'),'utf8'));
const amap=new Map(archive.assets.map(a=>[a.id,a]));
let rohAssets=[];try{rohAssets=JSON.parse(await readFile(path.join(out,'assets','archive','roh-manifest.json'),'utf8')).assets||[]}catch{}
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const src=id=>{const a=amap.get(id);return a?`assets/archive/${a.slug}-${a.defaultWidth}.${a.extension}`:null};
const home=await readFile(path.join(out,'index.html'),'utf8');
const rawHeader=home.match(/<header class="ff-header">[\s\S]*?<\/header>/i)?.[0];
const footer=home.match(/<footer class="ff-footer">[\s\S]*?<\/footer>/i)?.[0];
if(!rawHeader||!footer)throw new Error('Fan-first shell missing.');
const header=()=>`<a class="ff-skip" href="#content">Skip to content</a>${rawHeader.replaceAll(' aria-current="page"','')}`;
function head(title,description,canonical,robots='index, follow'){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#050505"><meta name="color-scheme" content="dark"><meta name="description" content="${esc(description)}"><meta name="robots" content="${robots}"><title>${esc(title)} — Belt Theory</title><link rel="icon" href="assets/mark.svg"><link rel="canonical" href="${canonical}"><meta property="og:site_name" content="Belt Theory"><meta property="og:title" content="${esc(title)} — Belt Theory"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${canonical}"><link rel="stylesheet" href="assets/fan-first.css"></head>`;}

const systems=[
 ['WWE','#d6d6d6','scorecard-wwe.html','More orderly. More concentrated.','Best evidence: every matched first-time men’s world champion held WWE gold first.'],
 ['AEW','#d9b441','scorecard-aew.html','Sharp crown. Crowded middle.','Best evidence: 82.1 flagship world-title stability, but only half of men’s world champions had prior AEW gold.'],
 ['TNA / Impact','#e34249','scorecard-tna.html','Strong ideas. Too many resets.','Best evidence: 51.6% TNA-created men’s world-title equity and multiple internal paths upward.'],
 ['WCW','#5aa3d8','scorecard-wcw.html','Great ladder. Bad finish.','Best evidence: nine of ten standard career-first world champions held earlier WCW-platform gold.'],
 ['Original ECW','#9d55c7','scorecard-ecw.html','Small system. Clear jobs.','Best evidence: six tag-before-world paths and only 4.9% imported-veteran world-title share.'],
 ['Ring of Honor','#d44752','scorecard-roh.html','Stable crown. Hidden tag ladder.','Best evidence: fourteen of 33 World champions held ROH Tag gold first.']
];
const systemRows=systems.map(([name,tone,href,verdict,evidence],i)=>`<a class="ff-system-row" href="${href}" style="--promotion:${tone}"><span>${String(i+1).padStart(2,'0')}</span><div><strong>${esc(name)}</strong><h2>${esc(verdict)}</h2><p>${esc(evidence)}</p></div><em>Open promotion →</em></a>`).join('');
const systemsPage=`${head('Championship systems','A side-by-side map of the six Belt Theory promotion systems, with plain-English strengths and warnings about what can and cannot be directly ranked.',`${origin}/systems-index`)}<body>${header()}<main id="content"><section class="ff-simple-hero"><div class="ff-shell"><p class="ff-section-label">Championship systems</p><h1>Six promotions.<br>Six different machines.</h1><p>This is a map, not a fake universal ranking. Each promotion has a different championship fingerprint. When we claim one beat another, we use a separate matched comparison with the same rules.</p></div></section><section class="ff-simple-section ff-shell"><div class="ff-system-warning"><strong>Important:</strong><p>TNA 73.2, AEW 68.2 and ROH 71.8 are internal diagnostic scores from different audits. They are useful inside each promotion. They are not a legitimate 1–2–3 ranking.</p><a class="ff-text-link" href="methodology.html">Why that matters →</a></div><div class="ff-system-list">${systemRows}</div></section><section class="ff-simple-section ff-translation-band"><div class="ff-shell"><div class="ff-method-card"><div><p class="ff-section-label">Want an actual winner?</p><h2>Use the direct comparisons.</h2><p>WWE vs. AEW, TNA vs. AEW and WWE vs. WCW vs. ECW use shared windows and shared rules. Those are the places where Belt Theory can honestly say who won.</p></div><div><a class="ff-button ff-button-primary" href="comparisons.html">Open big debates →</a></div></div></div></section></main>${footer}</body></html>`;

const policy=`${head('Update policy','How Belt Theory handles research corrections, new title reigns, methodology changes and historical source updates.',`${origin}/update-policy`)}<body>${header()}<main id="content"><section class="ff-simple-hero"><div class="ff-shell"><p class="ff-section-label">Update policy</p><h1>If the history changes,<br>the answer can change.</h1><p>Belt Theory treats the research as a living record. New reigns are added, factual mistakes are corrected, and published comparisons stay tied to the cutoff dates and rules they were built with.</p></div></section><section class="ff-simple-section ff-shell"><div class="ff-policy-grid"><article><span>01</span><h2>New title reigns</h2><p>Ongoing promotions keep creating new championship history. New research editions can extend the dataset without silently changing the cutoff of an older published comparison.</p></article><article><span>02</span><h2>Corrections</h2><p>If a reign date, lineage assignment, vacancy or classification is wrong, the underlying research should be corrected and any material conclusion affected by it should be rechecked.</p></article><article><span>03</span><h2>Method changes</h2><p>A changed formula is not quietly substituted into an old result. Material framework changes require a new version or a clearly labeled recalculation.</p></article><article><span>04</span><h2>Historical evidence</h2><p>Sources, photo rights and historical context can be strengthened over time. Authentic-history rules remain the same: no fake documentary evidence.</p></article></div><div class="ff-method-card"><div><p class="ff-section-label">What stays frozen</p><h2>A published matched comparison keeps its window.</h2><p>That is how “AEW 50.2 — WWE 49.8” remains an auditable statement about a specific seven-year period rather than a number that mysteriously changes every week.</p></div><div><a class="ff-button" href="research.html">See source data →</a></div></div></section></main>${footer}</body></html>`;

const allCredits=[...archive.assets,...rohAssets];
const creditRows=allCredits.map((a,i)=>{let image;if(a.slug&&a.defaultWidth){image=`assets/archive/${a.slug}-${a.defaultWidth}.${a.extension}`;}else{const v=a.variants?.find(x=>x.requestedWidth===a.defaultWidth)||a.variants?.at(-1);image=v?`assets/archive/${v.filename}`:null;}return `<article class="ff-credit-row"><span>${String(i+1).padStart(2,'0')}</span>${image?`<figure><img src="${image}" alt="${esc(a.alt||'Archival wrestling photograph')}" loading="lazy" decoding="async"></figure>`:''}<div><h2>${esc(a.caption||a.id)}</h2><p><strong>${esc(a.creator||'Source creator')}</strong> · ${esc(a.license||'License on source record')}</p><p>${esc(a.changes||'Responsive presentation only; photographed history remains unaltered.')}</p><a class="ff-text-link" href="${esc(a.sourcePage)}" rel="noreferrer" target="_blank">Open original source ↗</a></div></article>`}).join('');
const credits=`${head('Image credits','Photography sources, licenses and transformation disclosures for authentic historical images used by Belt Theory.',`${origin}/image-credits`,'noindex, follow')}<body>${header()}<main id="content"><section class="ff-simple-hero"><div class="ff-shell"><p class="ff-section-label">Image credits</p><h1>Real wrestling history.<br>Visible sources.</h1><p>When Belt Theory shows a real wrestler, event, crowd, venue or historical scene, the image comes from an authentic source record. The design may crop, resize or fade the photograph into the page; it does not fabricate the history.</p></div></section><section class="ff-simple-section ff-shell"><div class="ff-credit-rule"><strong>The visual rule</strong><p>AI may help with abstract design elements. It is not used to create fake documentary wrestling photographs or deceptive lookalikes presented as history.</p></div><div class="ff-credit-list">${creditRows}</div></section></main>${footer}</body></html>`;

const notFound=`${head('Page not found','The Belt Theory page you requested could not be found.',`${origin}/404`,'noindex, follow')}<body>${header()}<main id="content"><section class="ff-404"><div class="ff-shell"><span>404</span><p class="ff-section-label">Vacated title</p><h1>This page dropped the belt.</h1><p>The link may be old, mistyped or moved. The championship history is still here.</p><div><a class="ff-button ff-button-primary" href="index.html">Back to Belt Theory →</a><a class="ff-text-link" href="scorecards.html">Browse promotions</a></div></div></section></main>${footer}</body></html>`;

await writeFile(path.join(out,'systems-index.html'),systemsPage);
await writeFile(path.join(out,'update-policy.html'),policy);
await writeFile(path.join(out,'image-credits.html'),credits);
await writeFile(path.join(out,'404.html'),notFound);
console.log('Finished fan-first utility pages: Systems Index, Update Policy, Image Credits and 404.');
